<?php

class CondolencesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('condolences')->truncate();

		$condolences = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('condolences')->insert($condolences);
	}

}
