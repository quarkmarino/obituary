{{ ! Here’s the partial that we’ll use to show a obituary in a list. This is used on our index view. }}

<article data-toggle="view" data-target="obituaries/{{ id }}">
  <h3>{{ title }} {{ id }}</h3>
  <cite>{{ author_name }} on {{ created_at }}</cite>
</article>